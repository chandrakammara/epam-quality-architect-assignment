# Copy this file to `.env` in the same folder and fill in real values.
# Never commit the real `.env` file or real credentials.

# Base URL of the InvenTree instance under test.
BASE_URL=http://inventree.localhost

# Credentials for a valid InvenTree user account.
INVENTREE_USERNAME=your-username
INVENTREE_PASSWORD=your-password
